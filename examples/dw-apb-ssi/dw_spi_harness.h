#ifndef REHARNESS_DW_APB_SSI_HARNESS_H
#define REHARNESS_DW_APB_SSI_HARNESS_H

/* Auto-generated by LLM (reharness), 4 chunked parts */
/* ---- part 00 of 03 ---- */
/*
 * dw-apb-ssi driver scaffold — part 0 of 4
 * Userspace C reproduction of the Synopsys DesignWare APB SSI driver
 * MMIO register access pattern.
 */

#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* ------------------------------------------------------------------ */
/* Kernel macro stubs                                                 */
/* ------------------------------------------------------------------ */

#define BIT(n)              (1U << (n))
#define GENMASK(h, l)       ((~(0U)) << (l) & (~(0U)) >> (31 - (h)))
#define GENMASK_ULL(h, l)   (GENMASK(h, l))
#define BITS_PER_BYTE       8
#define BITS_PER_LONG       32
#define BITMASK(mask)       mask

#define __iomem
#define __packed            __attribute__((packed))
#define __aligned(x)        __attribute__((aligned(x)))
#define __force
#define __must_check        __attribute__((warn_unused_result))
#define __always_inline     static inline __attribute__((always_inline))
#define __cold              __attribute__((cold))

#define readl(addr)          harness_read32((uintptr_t)(addr))
#define writel(val, addr)   harness_write32((uint32_t)(val), (uintptr_t)(addr))
#define readw(addr)         harness_read16((uintptr_t)(addr))
#define writew(val, addr)   harness_write16((uint16_t)(val), (uintptr_t)(addr))
#define readb(addr)         harness_read8((uintptr_t)(addr))
#define writeb(val, addr)   harness_write8((uint8_t)(val), (uintptr_t)(addr))

#define ARRAY_SIZE(arr)      (sizeof(arr) / sizeof((arr)[0]))
#define min(a, b)            ((a) < (b) ? (a) : (b))
#define max(a, b)            ((a) > (b) ? (a) : (b))
#define clamp(val, lo, hi)  (max(min((val), (hi)), (lo)))

#define DIV_ROUND_UP(n, d)   (((n) + (d) - 1) / (d))
#define roundup(x, y)       ((((x) + (y) - 1) / (y)) * (y))

/* ------------------------------------------------------------------ */
/* Register offsets                                                    */
/* ------------------------------------------------------------------ */

#define DW_SPI_CTRLR0          0x00    /* 0   */
#define DW_SPI_CTRLR1          0x04    /* 4   */
#define DW_SPI_SSIENR          0x08    /* 8   */
#define DW_SPI_SER             0x10    /* 16  */
#define DW_SPI_BAUDR           0x14    /* 20  */
#define DW_SPI_TXFTLR          0x18    /* 24  */
#define DW_SPI_RXFTLR          0x1c    /* 28  */
#define DW_SPI_TXFLR           0x20    /* 32  */
#define DW_SPI_RXFLR           0x24    /* 36  */
#define DW_SPI_SR              0x28    /* 40  */
#define DW_SPI_IMR             0x2c    /* 44  */
#define DW_SPI_ISR             0x30    /* 48  */
#define DW_SPI_RISR            0x34    /* 52  */
#define DW_SPI_ICR             0x48    /* 72  */
#define DW_SPI_VERSION         0x5c    /* 92  */
#define DW_SPI_DR              0x60    /* 96  */
#define DW_SPI_RX_SAMPLE_DLY   0xf0    /* 240 */
#define DW_SPI_CS_OVERRIDE     0xf4    /* 244 */

#define MSCC_SPI_MST_SW_MODE   0x14    /* 20  */

/* ------------------------------------------------------------------ */
/* Bit-field definitions (from upstream driver)                        */
/* ------------------------------------------------------------------ */

/* CTRLR0 */
#define DW_SPI_CTRLR0_DFS_MASK        GENMASK(2, 0)
#define DW_SPI_CTRLR0_FRF_MASK        GENMASK(5, 4)
#define DW_SPI_CTRLR0_FRF_MOTO        (0 << 4)
#define DW_SPI_CTRLR0_FRF_TI         (1 << 4)
#define DW_SPI_CTRLR0_FRF_NAT        (2 << 4)
#define DW_SPI_CTRLR0_MODE_MASK      GENMASK(7, 6)
#define DW_SPI_CTRLR0_MODE_SCPH      BIT(6)
#define DW_SPI_CTRLR0_MODE_SCPOL     BIT(7)
#define DW_SPI_CTRLR0_SST_MASK       GENMASK(9, 8)
#define DW_SPI_CTRLR0_SST_NORM_ACT   (0 << 8)
#define DW_SPI_CTRLR0_SST_TWO_PHASE  (1 << 8)
#define DW_SPI_CTRLR0_SST_FEED_THEN  (2 << 8)
#define DW_SPI_CTRLR0_SST_ACT        (3 << 8)
#define DW_SPI_CTRLR0_SLV_OE         BIT(10)
#define DW_SPI_CTRLR0_TMOD_MASK      GENMASK(11, 10)
#define DW_SPI_CTRLR0_TMOD_TR        (0 << 10)
#define DW_SPI_CTRLR0_TMOD_TO        (1 << 10)
#define DW_SPI_CTRLR0_TMOD_RO       (2 << 10)
#define DW_SPI_CTRLR0_TMOD_EEPROM    (3 << 10)
#define DW_SPI_CTRLR0_MST_MASK       GENMASK(19, 11)
#define DW_SPI_CTRLR0_SER_MASK       GENMASK(25, 16)
#define DW_SPI_CTRLR0_SSTE          BIT(24)
#define DW_SPI_CTRLR0_SPI_FMOD_MASK  GENMASK(24, 24)
#define DW_SPI_CTRLR0_SPI_FMOD_FRF  (0 << 24)
#define DW_SPI_CTRLR0_SPI_FMOD_STD   (1 << 24)
#define DW_SPI_CTRLR0_SPI_FMOD_QUAD  (2 << 24)
#define DW_SPI_CTRLR0_SPI_FMOD_OCTAL (3 << 24)
#define DW_SPI_CTRLR0_INST_L_MASK   GENMASK(26, 25)
#define DW_SPI_CTRLR0_ADDR_L_MASK   GENMASK(28, 27)

/* SR */
#define DW_SPI_SR_BUSY               BIT(0)
#define DW_SPI_SR_TF_NOT_FULL        BIT(1)
#define DW_SPI_SR_RF_NOT_EMPT        BIT(3)
#define DW_SPI_SR_RF_FULL            BIT(4)
#define DW_SPI_SR_FRAME_END          BIT(5)

/* IMR / ISR / RISR / ICR */
#define DW_SPI_INT_TXEI             BIT(0)
#define DW_SPI_INT_TXOI             BIT(1)
#define DW_SPI_INT_RXUI             BIT(2)
#define DW_SPI_INT_RXOI             BIT(3)
#define DW_SPI_INT_RXFI             BIT(4)
#define DW_SPI_INT_FRIE             BIT(5)
#define DW_SPI_INT_MSTI              BIT(6)

/* SER */
#define DW_SPI_SER_SER_MASK         GENMASK(15, 0)

/* SSIENR */
#define DW_SPI_SSIENR_EN            BIT(0)

/* ------------------------------------------------------------------ */
/* Trace infrastructure                                                */
/* ------------------------------------------------------------------ */

static unsigned long trace_count = 0;

/* ------------------------------------------------------------------ */
/* Primitive stub implementations                                     */
/* ------------------------------------------------------------------ */

static uint8_t __attribute__((unused)) harness_read8(uintptr_t addr)
{
    uint8_t val = 0;
    printf("[trace %lu] R 0x%03lx = 0x%02x\n", trace_count++, (unsigned long)addr, val);
    return val;
}

static uint16_t __attribute__((unused)) harness_read16(uintptr_t addr)
{
    uint16_t val = 0;
    printf("[trace %lu] R 0x%03lx = 0x%04x\n", trace_count++, (unsigned long)addr, val);
    return val;
}

static uint32_t __attribute__((unused)) harness_read32(uintptr_t addr)
{
    uint32_t val = 0;
    printf("[trace %lu] R 0x%03lx = 0x%08x\n", trace_count++, (unsigned long)addr, val);
    return val;
}

static uint16_t __attribute__((unused)) harness_read16be(uintptr_t addr)
{
    uint16_t val = 0;
    printf("[trace %lu] R 0x%03lx = 0x%04x\n", trace_count++, (unsigned long)addr, val);
    return val;
}

static uint32_t __attribute__((unused)) harness_read32be(uintptr_t addr)
{
    uint32_t val = 0;
    printf("[trace %lu] R 0x%03lx = 0x%08x\n", trace_count++, (unsigned long)addr, val);
    return val;
}

static void __attribute__((unused)) harness_write8(uint8_t val, uintptr_t addr)
{
    printf("[trace %lu] W 0x%03lx = 0x%02x\n", trace_count++, (unsigned long)addr, val);
}

static void __attribute__((unused)) harness_write16(uint16_t val, uintptr_t addr)
{
    printf("[trace %lu] W 0x%03lx = 0x%04x\n", trace_count++, (unsigned long)addr, val);
}

static void __attribute__((unused)) harness_write32(uint32_t val, uintptr_t addr)
{
    printf("[trace %lu] W 0x%03lx = 0x%08x\n", trace_count++, (unsigned long)addr, val);
}

static void __attribute__((unused)) harness_write16be(uint16_t val, uintptr_t addr)
{
    printf("[trace %lu] W 0x%03lx = 0x%04x\n", trace_count++, (unsigned long)addr, val);
}

static void __attribute__((unused)) harness_write32be(uint32_t val, uintptr_t addr)
{
    printf("[trace %lu] W 0x%03lx = 0x%08x\n", trace_count++, (unsigned long)addr, val);
}

static void __attribute__((unused)) harness_write_w1c8(uint8_t val, uintptr_t addr)
{
    printf("[trace %lu] W1C 0x%03lx = 0x%02x\n", trace_count++, (unsigned long)addr, val);
}

static void __attribute__((unused)) harness_write_w1c16(uint16_t val, uintptr_t addr)
{
    printf("[trace %lu] W1C 0x%03lx = 0x%04x\n", trace_count++, (unsigned long)addr, val);
}

static void __attribute__((unused)) harness_write_w1c32(uint32_t val, uintptr_t addr)
{
    printf("[trace %lu] W1C 0x%03lx = 0x%08x\n", trace_count++, (unsigned long)addr, val);
}

/* ------------------------------------------------------------------ */
/* Device private struct                                              */
/* ------------------------------------------------------------------ */

struct dw_apb_ssi_priv {
    /* bind.state */
    uintptr_t               base;

    /* Upstream dw_spi fields */
    void                    *master;        /* spi_controller */
    void                    *target;        /* spi_target */
    char                    name[32];
    int                     bus_num;
    int                     num_cs;
    uintptr_t               *cs_override;  /* per-CS override */

    /* Register base (aka mmio base, may differ from base) */
    void                    *mmio;
    void                    *priv;          /* sub-driver private */

    /* Current transfer state */
    uint32_t                max_freq;       /* max bus frequency */
    uint32_t                cur_rx;         /* current RX sample threshold */
    uint32_t                cur_tx;         /* current TX sample threshold */
    uint32_t                cur_nbytes;     /* bytes per word */
    uint32_t                len;            /* remaining transfer length */
    uint32_t                tmod;           /* current transfer mode */

    /* FIFO thresholds */
    uint32_t                txftlr;         /* TX FIFO threshold */
    uint32_t                rxftlr;         /* RX FIFO threshold */
    unsigned int            fifo_len;       /* FIFO depth in entries */
    unsigned int            dfs;            /* data frame size */

    /* DMA */
    int                     dma_inited;     /* DMA init flag */
    void                    *tx_sgt;        /* TX scatter-gather */
    void                    *rx_sgt;        /* RX scatter-gather */
    void                    *dma_tx;        /* TX DMA channel */
    void                    *dma_rx;        /* RX DMA channel */
    void                    *dma_tx_burst;  /* TX burst len */
    void                    *dma_rx_burst;  /* RX burst len */

    /* IRQ / interrupt */
    unsigned int            irq;            /* IRQ number */
    uint32_t                imr;            /* current IMR shadow */

    /* Transfer buffer pointers */
    const void              *tx;            /* TX buffer */
    void                    *rx;            /* RX buffer */
    unsigned int            tx_len;          /* TX remaining */
    unsigned int            rx_len;          /* RX remaining */

    /* Configuration */
    uint32_t                (*get_cr0)(uint32_t dfs, uint32_t mode,
                                        uint32_t flags);
    uint32_t                (*get_tmod)(uint32_t tmod);
    uint32_t                (*get_acc_timing)(void);

    /* Enhanced / extended SPI */
    uint32_t                caps;           /* capability flags */
    uint32_t                version;        /* controller version */
    uint32_t                type;           /* controller type (DW SPI / DW APB SSI) */

    /* Platform / MMIO-specific */
    void                    *pdev;          /* platform device */
    void                    *paddr;         /* physical address */
    unsigned long           paddr_size;      /* mapped region size */
    int                     clk_enabled;     /* clock enable count */
    void                    *clk;            /* struct clk */
    void                    *clk_apb;        /* APB clock */
    void                    *clk_ssi;        /* SSI clock */
    void                    *dws;            /* back-pointer to dw_spi (mmio) */

    /* MSCC / Sparx5 / Elba / Alpine / HSSI / Intel / Mountevans / K210 */
    void                    *syscon;         /* regmap handle */
    uint32_t                syscon_idx;      /* regmap index */
    uint32_t                cs_overrides;    /* CS override mask */
    void                    *spi_en;         /* extra enable reg */
    uint32_t                spi_en_val;      /* enable value */
    void                    *extra;          /* sub-driver extra */
    uint32_t                (*set_cs)(void *spi, uint32_t enable);

    /* Memory-mapped execution */
    uint32_t                mem_op_ctrl0;    /* cached CTRLR0 for mem op */
    uint32_t                mem_op_ctrl1;    /* cached CTRLR1 for mem op */

    /* RX sample delay */
    uint32_t                rx_sample_dly;   /* RX sample delay value */
    uint32_t                def_thr;         /* default threshold */

    /* Error handling / reset */
    uint32_t                fifo_depth;      /* cached FIFO depth */
    uint32_t                reg_width;       /* register access width */
};

#endif /* REHARNESS_DW_APB_SSI_HARNESS_H */
