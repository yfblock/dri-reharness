# Generated RIS output (extractor artifacts)
output/

# Python bytecode & caches
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# drivers/ is a versioned evaluation corpus.
# linux/ is a pinned, initialized Git submodule (see .gitmodules).

# Editor / OS cruft
*.swp
*.swo
*~
.*.un~
.DS_Store

# Scratch planning doc (design captured in README)
plan.md

# QEMU/build artifacts (regenerable)
/initramfs_*.cpio.gz
/test_rootfs/
/test_rootfs_run/
/test_rootfs_plat/
/test/edu_trace_test
/test/gpio_trace_test
/kernel/build/
*.ko
*.o
*.mod
*.mod.c
*.cmd
Module.symvers
modules.order

# LaTeX intermediates (the source and final PDF remain versioned)
/paper/*.aux
/paper/*.log
/paper/*.out
/paper/*.fls
/paper/*.fdb_latexmk
/paper/comment.cut

# Node (Pi SDK synthesizer deps)
node_modules/
package-lock.json
