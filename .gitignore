# Generated RIS output (canonical path plus compatibility entry)
/artifacts/output/
/output

# Python bytecode & caches
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# benchmarks/drivers/ is the versioned evaluation corpus.
# vendor/linux/ is the pinned, initialized Git submodule (see .gitmodules).

# Editor / OS cruft
*.swp
*.swo
*~
.*.un~
.DS_Store

# Scratch planning doc (design captured in README)
plan.md

# QEMU/build artifacts (regenerable)
/initramfs_*.cpio.gz
# Canonical rootfs trees plus compatibility entries
/platform/rootfs/
/test_rootfs
/test_rootfs_run
/test_rootfs_plat
/test/edu_trace_test
/test/gpio_trace_test
/platform/kernel/build/
/kernel/build/
*.ko
*.o
*.mod
*.mod.c
*.cmd
Module.symvers
modules.order

# LaTeX intermediates (the source and final PDF remain versioned)
/research/paper/*.aux
/research/paper/*.log
/research/paper/*.out
/research/paper/*.fls
/research/paper/*.fdb_latexmk
/research/paper/comment.cut
/paper/*.aux
/paper/*.log
/paper/*.out
/paper/*.fls
/paper/*.fdb_latexmk
/paper/comment.cut

# Pi SDK dependencies live with the Pi tool
/tools/pi/node_modules/
/tools/pi/package-lock.json
